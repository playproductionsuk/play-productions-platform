const key="playCart";export function getCart(){try{return JSON.parse(localStorage.getItem(key)||"[]")}catch{return[]}}export function addToCart(item){const cart=getCart(),existing=cart.find(x=>x.id===item.id&&x.type===item.type);if(existing)Object.assign(existing,item);else cart.push(item);localStorage.setItem(key,JSON.stringify(cart));window.dispatchEvent(new Event("cartchange"));return cart}export function removeFromCart(id){localStorage.setItem(key,JSON.stringify(getCart().filter(x=>x.id!==id)));window.dispatchEvent(new Event("cartchange"))}export function cartTotal(){return getCart().reduce((sum,item)=>sum+Number(item.price||0),0)}
