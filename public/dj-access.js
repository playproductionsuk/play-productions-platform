import{createEnquiry}from"./platform-data.js";const form=document.querySelector("#djAccessForm"),status=document.querySelector("#djAccessStatus");form.addEventListener("submit",async event=>{event.preventDefault();const button=form.querySelector("button");button.disabled=true;status.textContent="Sending your request…";try{const values=Object.fromEntries(new FormData(form));values.mailingConsent=document.querySelector('input[name="mailingConsent"]').checked;await createEnquiry({...values,type:"dj-access"});form.reset();status.textContent="Request received. I’ll review it and send login details if approved."}catch(error){console.error(error);status.textContent="That didn’t send. Please try again shortly."}finally{button.disabled=false}});
