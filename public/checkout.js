import{getCart,cartTotal}from"./cart.js";const items=getCart(),list=document.querySelector("#checkoutItems");list.innerHTML=items.length?items.map(i=>`<article class="checkout-row"><img src="${i.artwork||"icons/fallback.png"}" alt=""><div><strong>${i.title}</strong><br><small>Personal digital download</small></div><strong>£${Number(i.price).toFixed(2)}</strong></article>`).join("")+`<div class="cart-total"><span>Total</span><strong>£${cartTotal().toFixed(2)}</strong></div>`:'<p class="empty">Your cart is empty. <a class="text-link" href="music.html">Browse music →</a></p>';document.querySelectorAll("#stripePlaceholder,#paypalPlaceholder").forEach(button=>button.addEventListener("click",()=>{document.querySelector("#checkoutMessage").textContent="Payment is intentionally in preview mode. Connect and test the live provider secrets before accepting orders."}));
