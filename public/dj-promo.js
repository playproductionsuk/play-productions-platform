import{firebaseApp,firebaseReady,db,escapeHtml,normaliseTrack,loadTracks}from"./platform-data.js";import{getAuth,onAuthStateChanged,signOut}from"https://www.gstatic.com/firebasejs/11.9.1/firebase-auth.js";import{collection,doc,getDoc,getDocs,query,where}from"https://www.gstatic.com/firebasejs/11.9.1/firebase-firestore.js";const list=document.querySelector("#djTrackList"),tools=document.querySelector(".store-tools"),dock=document.querySelector("#playerDock"),player=document.querySelector("#audioPlayer"),playerTitle=document.querySelector("#playerTitle"),playerCover=document.querySelector("#playerCover"),auth=firebaseApp?getAuth(firebaseApp):null,demo=new URLSearchParams(location.search).get("demo")==="1";let tracks=[],user;tools.insertAdjacentHTML("beforeend",'<label><span class="sr-only">Genre</span><select id="djGenre"><option value="">All genres</option></select></label><label><span class="sr-only">Sort</span><select id="djSort"><option value="newest">Newest first</option><option value="az">A–Z</option></select></label>');if(demo)document.querySelector("main").insertAdjacentHTML("afterbegin",'<div class="dj-preview-banner"><strong>Approved DJ portal preview</strong><br>Sample catalogue and disabled downloads. No account or private file access is being used.</div>');async function allowed(account){const profile=await getDoc(doc(db,"users",account.uid));return profile.exists()&&profile.data().djAccess===true}async function loadPromos(){if(demo||!firebaseReady)tracks=(await loadTracks()).filter(t=>t.showInDjPool);else{const snapshot=await getDocs(query(collection(db,"tracks"),where("showInDjPool","==",true)));tracks=snapshot.docs.map(item=>normaliseTrack({id:item.id,...item.data()}))}[...new Set(tracks.map(t=>t.style).filter(Boolean))].forEach(v=>document.querySelector("#djGenre").add(new Option(v,v)));render()}function render(){const q=document.querySelector("#djSearch").value.toLowerCase(),genre=document.querySelector("#djGenre").value;let shown=tracks.filter(t=>(!genre||t.style===genre)&&(!q||`${t.title} ${t.style} ${(t.moodTags||[]).join(" ")}`.toLowerCase().includes(q)));shown.sort((a,b)=>document.querySelector("#djSort").value==="az"?a.title.localeCompare(b.title):String(b.releaseDate).localeCompare(String(a.releaseDate)));list.innerHTML=shown.length?shown.map(t=>`<article class="dj-track store-track" data-id="${escapeHtml(t.id)}"><img src="${escapeHtml(t.coverUrl||"icons/fallback.png")}" alt="Cover art for ${escapeHtml(t.title)}"><div class="dj-track-info"><p>${escapeHtml([t.style,t.bpm&&`${t.bpm} BPM`,t.key].filter(Boolean).join(" · "))}</p><h3>${escapeHtml(t.title)}</h3><small>${escapeHtml(t.teaser||"")}</small></div>${t.previewUrl?'<button class="dj-preview" type="button">Preview</button>':""}<div class="format-actions"><button class="button ghost dj-download" data-format="mp3">MP3 ↓</button><button class="button primary dj-download" data-format="wav">WAV ↓</button></div></article>`).join(""):'<p class="empty">No promo tracks match those filters.</p>'}list.addEventListener("click",async e=>{const article=e.target.closest(".dj-track"),t=tracks.find(x=>String(x.id)===article?.dataset.id);if(!t)return;if(e.target.closest(".dj-preview")){player.src=t.previewUrl;playerTitle.textContent=t.title;playerCover.src=t.coverUrl||"icons/fallback.png";dock.hidden=false;player.play().catch(()=>{})}const download=e.target.closest(".dj-download");if(download){if(demo||!firebaseReady){download.textContent="Demo only";return}download.disabled=true;try{const token=await user.getIdToken(),response=await fetch(`/api/dj-download?track=${encodeURIComponent(t.id)}&format=${download.dataset.format}`,{headers:{Authorization:`Bearer ${token}`}}),data=await response.json();if(!response.ok)throw new Error(data.error);location.href=data.url}catch(error){download.textContent=error.message;download.disabled=false}}});[document.querySelector("#djSearch"),document.querySelector("#djGenre"),document.querySelector("#djSort")].forEach(x=>x.addEventListener(x.tagName==="INPUT"?"input":"change",render));document.querySelector("#closePlayer").onclick=()=>{player.pause();dock.hidden=true};document.querySelector("#djSignOut").onclick=async()=>{if(auth)await signOut(auth);location.href="dj-login.html"};if(demo||!firebaseReady)loadPromos();else onAuthStateChanged(auth,async account=>{if(!account||!await allowed(account)){if(account)await signOut(auth);location.replace("dj-login.html");return}user=account;await loadPromos()});
